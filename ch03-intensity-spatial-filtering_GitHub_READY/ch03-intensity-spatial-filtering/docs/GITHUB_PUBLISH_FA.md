# قرار دادن این فصل در Repository اصلی

Repository اصلی:

`https://github.com/hadisadoghiyazdi1971/digital-image-processing-teaching-toolbox`

در نسخه محلی Repository این ساختار را بسازید:

```text
digital-image-processing-teaching-toolbox/
└── chapters/
    └── ch03-intensity-spatial-filtering/
```

تمام محتویات همین پوشه را بدون لایه اضافی داخل `chapters/ch03-intensity-spatial-filtering/` کپی کنید.

سپس در GitHub Desktop:

1. Summary: `Add Chapter 3 intensity transformations and spatial filtering toolbox`
2. `Commit to main`
3. `Push origin`

پوشه‌های `.venv`، `wheels` و خروجی‌های موقت را کپی نکنید؛ `.gitignore` نیز برای جلوگیری از اضافه‌شدن آنها تنظیم شده است.
